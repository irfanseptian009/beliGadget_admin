/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
	dev: {
	  host: 'localhost', // Ganti dengan host yang Anda inginkan
	  port: 3001,        // Ganti dengan port yang Anda inginkan
	},
  };